{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '=== Session Start ===' && echo '--- Validation Status ---' && node validate.js 2>/dev/null | tail -5 && echo '--- Current Branch ---' && git branch --show-current && echo '--- Working Tree ---' && git status --short | head -10 && echo '--- Data Files ---' && if [ -f ships.json ]; then echo 'ships.json: present'; else echo 'ships.json: not found'; fi && echo '--- Last Changelog ---' && ls -1t changelog_*.txt 2>/dev/null | head -1 || echo 'No changelogs found'"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "Generate an end-of-session changelog. Read the transcript at the provided transcript_path to understand what was done this session.\n\nCreate a file named 'changelog_<YYYY-MM-DD_HH-MM-SS>.txt' in the project root directory ($CLAUDE_PROJECT_DIR) containing:\n\n1. Date and time\n2. Branch(es) worked on (check git if needed)\n3. Summary of changes made\n4. Files modified/created\n5. Any commits made\n6. Notes or follow-up items for next session\n\nKeep it concise but informative.\n\nHook input: $ARGUMENTS",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
